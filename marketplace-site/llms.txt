# The Marketplace directory — marketplace.bitcoineconomy.ai

> The agent-readable directory of services autonomous AI agents buy and sell for Bitcoin —
> inference, compute, machine work, verification, commerce bridges, swaps, liquidity, and fiat ramps.
> Curated registry + a live snapshot of Nostr-announced inventory (Routstr providers, NIP-87 ecash mints)
> + a probed cross-provider price index for inference.

## How to consume this directory (five fetches, no inference needed)

1. https://marketplace.bitcoineconomy.ai/directory.json — the curated registry. Filter locally on category, payment_methods,
   automatability (api-no-account | api-account | api-kyc), and kyc. Entries carry auth, quickstart,
   and (where verified) api_base + pricing_url — enough to make the first call.
2. https://marketplace.bitcoineconomy.ai/live/snapshot.json — what announces itself on Nostr right now (Routstr kind-38421 inference
   providers, NIP-87 ecash mints, kind-38000 reviews). Each provider carries probe status
   (alive | unreachable | unverified-tor-only | unroutable — announcements outlive nodes; filter status === "alive"
   unless you can reach Tor, where network: tor | both endpoints are yours to verify) plus latency_ms,
   model_count, and accepted mints.
3. https://marketplace.bitcoineconomy.ai/live/models.json — the cross-provider inference price index: model id → every alive provider
   serving it, cheapest first, in sats per token (+ max_cost per request, the budgeting ceiling).
   One fetch answers "who serves model X cheapest right now".
4. https://marketplace.bitcoineconomy.ai/live/l402index.json — the WIDER L402 tier: a selective, attributed pass over 402index.io's
   verified-L402 feed (Ryan Gentry, ex-Lightning Labs) — real Lightning-payable (L402) endpoints beyond the
   curated set (health-filtered, reliability-capped), each with a source_page back to 402index. provenance:
   external-index — third-party-indexed + verified, NOT our endorsement. Static fallback at https://marketplace.bitcoineconomy.ai/l402index.json.
5. https://marketplace.bitcoineconomy.ai/live/uptime.json — rolling uptime history for every probed target, INCLUDING the marketplace's own
   surfaces (self:* rows — no green by assertion). RECOMPUTABLE, NOT A SCORE: the doc carries the raw per-run
   observations (runs[]), the exact formula, and explicit denominators — recompute any stat rather than trust it.
   Snapshot digests are Nostr-signed + Bitcoin-anchored nightly (OpenTimestamps) — anchor records at /anchors/index.json.

## The tool catalog (what an agent EQUIPS, vs the registry above of what it BUYS)

https://marketplace.bitcoineconomy.ai/tools.json — the equipment an agent installs or runs to transact: wallets & treasuries, node
toolkits, ecash software, bridges & swaps, and the protocol primitives (L402, NWC, BOLT12, LNURL, MCP).
Each tool carries toolbox_group, tool_type, prereq_tier (what must be in place first), repo/docs links, and
mcp_endpoint where it ships its own MCP server. Full per-tool detail (gotchas, dependencies) at its card_url.

## Or call the directory as tools (MCP)

An MCP server at https://marketplace.bitcoineconomy.ai/mcp exposes both the service registry and the tool catalog as Model Context Protocol
tools, so an agent can call instead of fetch: find_service, get_service, price_model, list_categories, get_quote, get_uptime
(a ready-to-pay payment plan — a live L402 invoice or live sats price where the provider supports it),
find_tool, get_tool, and list_mcp_servers. That last one lists the providers here that run their OWN MCP server
(Amboss, Bitrefill, Alby NWC) — discover here, connect there to act. Stateless Streamable HTTP: POST one
JSON-RPC request, get one JSON response. No funds move through it; you pay providers directly.

## List your service (the directory is two-sided)

Run a service an agent can pay for in Bitcoin? List it yourself — no account, no UI, no fee. Publish a
signed Nostr "agent-payable service announcement" (kind 38555, our microstandard; reuse Routstr
kind 38421 if the service is inference). It appears in the announced tier at https://marketplace.bitcoineconomy.ai/live/announced.json on
the next 6-hourly refresh — with a liveness probe + trust signals (announcement age, accepted-mint health) —
and graduates to the curated registry above via editor verification. Announced is permissionless and labeled:
taken as published, not endorsed. Field schema + a copyable example event: https://marketplace.bitcoineconomy.ai/spec/agent-payable-service-announcement.md
(JSON schema: https://marketplace.bitcoineconomy.ai/spec/agent-payable-service-announcement.schema.json).

## Legacy / non-MCP agents

An OpenAPI 3.0 description of the GET routes above is at https://marketplace.bitcoineconomy.ai/openapi.json (operationIds:
getDirectory, getToolCatalog, getLiveSnapshot, getPriceIndex, getUptimeHistory, getEntry), with the OpenAI-plugin-era
manifest at https://marketplace.bitcoineconomy.ai/.well-known/ai-plugin.json. Read-only, no auth; you pay each provider directly.

Static fallbacks (work without the worker): https://marketplace.bitcoineconomy.ai/snapshot.json + https://marketplace.bitcoineconomy.ai/models.json + https://marketplace.bitcoineconomy.ai/l402index.json
Part of: https://bitcoineconomy.ai — the case for a Bitcoin-centric AI agent economy (manifest: https://bitcoineconomy.ai/llms.txt)

Every entry below has a clean Markdown route. provenance: curated = editor-verified against primary
sources on the date shown; the live snapshot carries provenance live-from-relay (signed Nostr events,
taken as published — verify before trusting); the price index carries provenance
probed-from-provider-endpoints (the providers’ own published numbers, not endorsements).
Note honestly: the kind-38000 reviews on the relays today target ecash mints, not inference providers.

## Inference (LLM/API calls an agent pays for per request)

- [Routstr](https://marketplace.bitcoineconomy.ai/entries/routstr.md): LLM inference — OpenAI-compatible API calls from independent providers. Payment: cashu/lightning; KYC: none; automatability: api-no-account.
- [PPQ.AI (PayPerQ)](https://marketplace.bitcoineconomy.ai/entries/ppq-ai.md): Pay-per-query frontier-model calls — chat, image, video, audio. Payment: l402/lightning; KYC: none (Lightning path); automatability: api-no-account.
- [Maple AI](https://marketplace.bitcoineconomy.ai/entries/maple.md): Private TEE-encrypted LLM inference through an OpenAI-compatible API — prompts provably unreadable to the provider. Payment: fiat; KYC: none; automatability: api-account.

## Compute (servers an agent provisions and pays for in sats)

- [LNVPS](https://marketplace.bitcoineconomy.ai/entries/lnvps.md): VPS compute — provision a server with Nostr login and fund it in sats. Payment: lightning/nwc; KYC: none; automatability: api-no-account.
- [BitLaunch](https://marketplace.bitcoineconomy.ai/entries/bitlaunch.md): VPS on DigitalOcean / Vultr / Linode capacity, provisioned end-to-end by API. Payment: lightning/onchain; KYC: none; automatability: api-account.

## Machine work (agents buying and selling work)

- [OpenAgents](https://marketplace.bitcoineconomy.ai/entries/openagents.md): Inference + machine work — call the Khala OpenAI-compatible gateway (openagents/khala) and pay per call over Lightning; or buy and sell labor, compute, and data across five markets, settled in Bitcoin with verifiable evidence. Payment: lightning; KYC: none; automatability: api-no-account.

## Verification (independent pre-action verdicts + signed proofs an agent pays per call)

- [invinoveritas](https://marketplace.bitcoineconomy.ai/entries/invinoveritas.md): Independent pre-action verdicts and signed proofs — a neutral approve/concerns/reject on a trade, diff, plan, command, or on-chain action before an irreversible step (~200 sats), a portable schnorr-signed audit proof after (100 sats), and free no-auth verification of any counterparty's proof. Payment: l402/lightning/fiat; KYC: none; automatability: api-no-account.

## Commerce bridge (paying non-Bitcoin merchants with sats)

- [Unhuman](https://marketplace.bitcoineconomy.ai/entries/unhuman.md): Real-world goods bought end to end over Lightning - roasted-to-order coffee (Unhuman Coffee) and domain registration (Unhuman Domains); Unhuman Store is a hub listing further agent storefronts. Payment: l402/lightning; KYC: none; automatability: api-no-account.
- [Bitrefill](https://marketplace.bitcoineconomy.ai/entries/bitrefill.md): Agent-relevant slice: prepaid virtual Visa/Mastercard (the universal card bridge), eSIM data, phone/VoIP refills, VPN subscriptions. The bulk of the catalog is human-consumption gift cards (A2P procurement). NOT in the catalog: VPS/hosting, domains, compute, AI credits — see lnvps/bitlaunch/routstr/ppq-ai for those. Payment: lightning/onchain; KYC: none; automatability: api-account.

## Privacy / connectivity

- [Mullvad VPN](https://marketplace.bitcoineconomy.ai/entries/mullvad.md): VPN time on a no-identity account — fund a random 16-digit account number with sats. Payment: lightning/onchain; KYC: none; automatability: api-account.

## Swaps (non-custodial, no-KYC asset conversion)

- [Boltz](https://marketplace.bitcoineconomy.ai/entries/boltz.md): Atomic swaps — Lightning ↔ on-chain ↔ Liquid, plus Lightning ↔ USDT/USDC. Payment: lightning/onchain/liquid; KYC: none; automatability: api-no-account.
- [SideShift.ai](https://marketplace.bitcoineconomy.ai/entries/sideshift.md): Cross-asset swaps across 200+ assets and 45+ networks, direct to wallet. Payment: lightning/onchain; KYC: none; automatability: api-no-account.
- [Flashnet](https://marketplace.bitcoineconomy.ai/entries/flashnet.md): BTC ↔ stablecoin swaps (USDT, USDB, USDC) on a Bitcoin-native AMM. Payment: spark/lightning; KYC: none; automatability: api-no-account.
- [SideSwap](https://marketplace.bitcoineconomy.ai/entries/sideswap.md): L-BTC ↔ L-USDt and direct asset-to-asset swaps on Liquid order books. Payment: liquid; KYC: none; automatability: api-no-account.

## Liquidity (Lightning channel/balance management)

- [Loop](https://marketplace.bitcoineconomy.ai/entries/loop.md): Lightning ↔ on-chain liquidity — non-custodial submarine swaps for LND nodes, fee for service. Payment: lightning/onchain; KYC: none; automatability: api-no-account.
- [Amboss (Magma)](https://marketplace.bitcoineconomy.ai/entries/amboss.md): Inbound Lightning liquidity — buy a channel opened to your node so the agent can receive payments; sized in dollars, leased for a term, paid in sats. Payment: lightning/onchain; KYC: none; automatability: api-no-account.
- [Amboss Rails](https://marketplace.bitcoineconomy.ai/entries/rails.md): Sell-side liquidity yield — deploy idle self-custodied bitcoin as Lightning routing liquidity and earn fees on the payment flow that crosses it (the provide-side complement to Magma's buy-side). Payment: lightning; KYC: n/a; automatability: api-account.

## Fiat ramps (custodial venues at the border; KYC noted per entry)

- [Strike](https://marketplace.bitcoineconomy.ai/entries/strike.md): Lightning ↔ fiat conversion at the custodial edge. Payment: lightning/onchain/fiat; KYC: required; automatability: api-kyc.
- [River](https://marketplace.bitcoineconomy.ai/entries/river.md): Fiat ↔ BTC (US) plus Lightning payments via the RLS API. Payment: lightning/onchain/fiat; KYC: required; automatability: api-kyc.
- [Kraken](https://marketplace.bitcoineconomy.ai/entries/kraken.md): Fiat ↔ BTC ↔ stablecoin across many fiat currencies on a mature trading API. Payment: lightning/onchain/fiat; KYC: required; automatability: api-kyc.
- [Coinbase](https://marketplace.bitcoineconomy.ai/entries/coinbase.md): Fiat ↔ BTC ↔ USDC via a mature developer platform. Payment: lightning/onchain/fiat; KYC: required; automatability: api-kyc.
